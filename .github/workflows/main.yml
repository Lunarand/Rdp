name: RDP (Pinggy Stable - Fast Boot)

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Configure Core RDP Settings
        run: |
          # --- AUDIO & SESSION FIXES ---
          Set-Service -Name Audiosrv -StartupType Automatic
          sc failure Audiosrv reset=86400 actions=restart/5000/restart/10000/restart/30000
          Start-Service -Name Audiosrv
          Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services' -Name "fDisableAudioCapture" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services' -Name "MaxDisconnectionTime" -Value 0 -Force

          # Enable clipboard sharing
          Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services' -Name "fDisableClip" -Value 0 -Force

          # Enable Remote Desktop (disable NLA for log-based interaction)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force

          # Enable auto-reconnect on disconnect
          Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services' -Name "fEnableAutoReconnect" -Value 1 -Force

          # Firewall
          netsh advfirewall firewall delete rule name="RDP-Tailscale" 2>$null
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

          # Auto-restart RDP service if it crashes
          sc failure TermService reset=86400 actions=restart/5000/restart/10000/restart/30000
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        env:
          MY_SECRET_PASS: ${{ secrets.PASS }}
        run: |
          if (-not $env:MY_SECRET_PASS) {
            Write-Error "Secret 'PASS' is missing. Add it in Repository Settings."
            exit 1
          }
          $securePass = ConvertTo-SecureString $env:MY_SECRET_PASS -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          echo "RDP_CREDS=User: RDP | Password: (Hidden)" >> $env:GITHUB_ENV

      - name: Optimize System (Fast)
        run: |
          # High Performance power plan
          powercfg /setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c

          # Disable Windows Automatic Updates (prevents background lag)
          New-Item -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate\AU" -Force | Out-Null
          Set-ItemProperty -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate\AU" -Name "NoAutoUpdate" -Value 1 -Type DWord -Force

          Write-Host "Core system optimization complete."

      # ---------------------------------------------------------------
      # Pinggy Tunnel (works on Windows exactly like the Linux ssh command)
      # ---------------------------------------------------------------
      - name: Start Pinggy Tunnel & Maintain Connection
        run: |
          # OpenSSH client is pre-installed on windows-latest
          if (-not (Get-Command ssh.exe -ErrorAction SilentlyContinue)) {
            Write-Host "Installing OpenSSH client..."
            Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0
          }

          function Get-PinggyAddress($stdoutLog, $stderrLog, $timeoutSec = 60) {
            $start = Get-Date
            while ((Get-Date) - $start -lt [TimeSpan]::FromSeconds($timeoutSec)) {
              foreach ($lf in @($stdoutLog, $stderrLog)) {
                if (Test-Path $lf) {
                  $m = (Get-Content $lf -Tail 30) | Select-String 'tcp://([a-zA-Z0-9\.\-_]+:\d+)'
                  if ($m) { return $m.Matches.Groups[1].Value.Trim() }
                }
              }
              Start-Sleep -Seconds 2
            }
            return $null
          }

          $maxRetries = 5
          $delay = 10

          for ($attempt = 1; $attempt -le $maxRetries; $attempt++) {
            Write-Host "`n=== Pinggy attempt $attempt/$maxRetries ==="

            $stdoutLog = "$env:TEMP\pinggy_stdout_$attempt.log"
            $stderrLog = "$env:TEMP\pinggy_stderr_$attempt.log"

            $proc = Start-Process -FilePath ssh.exe `
              -ArgumentList "-o StrictHostKeyChecking=no -p 443 -R0:localhost:3389 -N tcp@a.pinggy.io" `
              -NoNewWindow `
              -RedirectStandardOutput $stdoutLog `
              -RedirectStandardError  $stderrLog `
              -PassThru

            $address = Get-PinggyAddress -stdoutLog $stdoutLog -stderrLog $stderrLog -timeoutSec 60

            if ($address) {
              Write-Host "`n=========================================="
              Write-Host "  RDP ACCESS READY"
              Write-Host "  Address : $address"
              Write-Host "  Username: RDP"
              Write-Host "  Password: (from your PASS secret)"
              Write-Host "==========================================`n"

              $delay = 10

              while ($true) {
                Start-Sleep -Seconds 30
                if ($proc.HasExited) {
                  Write-Host "[$(Get-Date -Format 'HH:mm:ss')] Pinggy tunnel exited (code $($proc.ExitCode)). Restarting..."
                  break
                }
                Write-Host "[$(Get-Date -Format 'HH:mm:ss')] RDP active via Pinggy — $address"
              }
            } else {
              Write-Warning "Failed to get Pinggy address on attempt $attempt."
              if ($proc -and -not $proc.HasExited) { $proc.Kill() }
              foreach ($lf in @($stdoutLog, $stderrLog)) {
                if (Test-Path $lf) {
                  Write-Host "--- $lf (last 20 lines) ---"
                  Get-Content $lf -Tail 20 | ForEach-Object { Write-Host $_ }
                  Write-Host "---------------------------"
                }
              }
              Write-Host "Retrying in ${delay}s..."
              Start-Sleep -Seconds $delay
              $delay = [Math]::Min($delay * 2, 120)
            }
          }

          Write-Error "All $maxRetries Pinggy attempts failed. See logs above."

      # 9. Auto-Restart Workflow (as requested)
      # ⚠️ WARNING: GITHUB_TOKEN cannot trigger a new workflow_dispatch event.
      # The command will succeed but the new run WILL NOT START.
      # To actually restart the workflow, use a Personal Access Token (MY_PAT) instead.
      - name: 🔄 Auto-Restart Server
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Triggering next workflow run to restart the server..."
          gh workflow run main.yml --ref ${{ github.ref_name }}
          echo "✅ Restart triggered successfully! Thanks for using @GamingBurst007's script!"
